import React from 'react';
import GamesSchedule from '@/features/game-scheduling/components/GamesSchedulePage';

/**
 * Games Schedule Page
 * 
 * Thin wrapper for the Games Schedule feature component.
 * Part of the pages layer that handles routing.
 */
export default function GamesSchedulePage() {
  return <GamesSchedule />;
}
